--Create table
--lets create two table

create table employee(
fname varchar(50),
lname varchar(50),
employeeid int,
address varchar(30),
city varchar(35)
);
--Did you see, In oracle we can create table with small alphabet too
--Now lets create another table with constraint and primary key

create table department(
departmentcode int,
departmentname varchar(50),
employeeid int,
CONSTRAINT pk_department_departmentcode primary key(departmentcode)
);

--see this table also created, here i am showing by adding primary key and 
--constraint is capital other are small means oracle is not case sensetive in 
--coding but when returning output table name should be exact. 

--Now we can see we have primary key in second table but not in first so lets 
--add using alter

alter table employee 
add constraint pk_employee_employeeid primary key(employeeid);

--suppose we just say above oracle is case sensetive means we can have small
--and capital letter in code not in table and column name, lets add foreign key
--in second table and see above example too


alter table department
add constraint fk_department_EMPLOYEEID foreign key(employeeid) references employee(employeeid);

--Now lets insert value in both table
-- Inserting data into the 'employee' table
--NOTE like in other sql, mysql oracle does not support inserting multiple
--table at once. 

INSERT INTO employee (fname, lname, employeeid, address, city)
VALUES ('John', 'Doe', 1, '123 Main St', 'New York');

INSERT INTO employee (fname, lname, employeeid, address, city)
VALUES ('Jane', 'Smith', 2, '456 Elm St', 'Los Angeles');

INSERT INTO employee (fname, lname, employeeid, address, city)
VALUES ('Bob', 'Johnson', 3, '789 Oak St', 'Chicago');

INSERT INTO employee (fname, lname, employeeid, address, city)
VALUES ('Alice', 'Williams', 4, '101 Pine St', 'Houston');

INSERT INTO employee (fname, lname, employeeid, address, city)
VALUES ('Michael', 'Brown', 5, '222 Cedar St', 'Miami');

-- Inserting data into the 'department' table
INSERT INTO department (departmentcode, departmentname, employeeid)
VALUES (101, 'HR', 1);

INSERT INTO department (departmentcode, departmentname, employeeid)
VALUES (102, 'IT', 2);

INSERT INTO department (departmentcode, departmentname, employeeid)
VALUES (103, 'Finance', 3);

INSERT INTO department (departmentcode, departmentname, employeeid)
VALUES (104, 'Sales', 4);

INSERT INTO department (departmentcode, departmentname, employeeid)
VALUES (105, 'Marketing', 5);

--now lets see all values in employee table
--lets add select capital and from small just to show, it will also work
SELECT * from employee;
